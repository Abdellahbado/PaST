# Active Iteration

Current active iteration:

- `iterations/20260421_phaseR_epsilon_warmstart_diagnostic/`

Reason:

- Phase Q showed that synthetic-only learned move ranking gives only marginal lift over the strongest handcrafted screening baseline.
- Current focus is a controlled epsilon warm-start diagnostic on benchmark instance 61 to test whether cumulative cross-epsilon search, rather than better screening, is the real missing lever.
