# Problem

We need to determine whether the single-machine DP can support a credible new heuristic direction for BPMSTP.

The exact-DP global directions already failed as primary lines. The remaining realistic question is narrower:

- if a machine assignment is fixed, does replacing the paper's ESR with our single-machine DP improve TEC materially?

This iteration is strictly about that question.
