# Problem

Phase G question:

- can a restricted master + pricing-style loop provide meaningful regime-2 signal on instance `61` at fixed epsilon?

Bounded target:

- instance `61`
- epsilon `347` primary
- no frontier, no broad benchmark

Evaluation focus:

- feasibility of restricted decomposition workflow
- LP bound informativeness
- quality gap vs EHS/reference at same epsilon
