# Blockers

- `g12345678910` likely suffers from workflow/policy inefficiency rather than a
  clearly established hardness wall.
- `g37` currently looks misrouted or under-tested under the intended K=2 Step-3
  exact profile-realization path.
- High-`n` robustness failures (`std::length_error`) still limit some other
  families, but they are not the main active branch of this iteration.
