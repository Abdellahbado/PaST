# Problem

The active work is no longer general archive cleanup or K=4 stabilization.

The active benchmark questions are:

1. why the easy dense family `g12345678910 = {1..10}` times out from `n=5000`
   onward even though it is contiguous, contains `1`, and is exact through
   `n=3500` via Step 2 / `ffd`
2. whether `g37 = {3,7}` is a true hardness wall, or whether it was simply run
   under the wrong routing and missed the intended K=2 Step-3 exact
   profile-realization path

The current thread also needs a small, durable current-facing layer so a fresh
conversation can recover context quickly.
