# Runtime requirements for the PaST codepath.
#
# Note on PyTorch:
# - Install an appropriate build for your machine (CPU/GPU) and platform.
# - See https://pytorch.org/get-started/locally/

# ── Core ──────────────────────────────────────────────────
numpy>=1.24
PyYAML>=6.0
tqdm>=4.66
gymnasium>=0.29

# Torch is intentionally unpinned (platform-specific wheels)
torch

# ── Data / ML ─────────────────────────────────────────────
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
lightgbm>=4.0
matplotlib>=3.7

# ── Accelerators (Cython sparse-DP + Numba fallback) ─────
Cython>=3.0
numba>=0.58

# ── G-LNS (LLM-driven evolutionary ALNS) ─────────────────
groq>=0.28.0
pydantic>=2.0

# ── Dev / testing ─────────────────────────────────────────
pytest>=7.0
